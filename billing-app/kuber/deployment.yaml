apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-app
  namespace: mantogin-hw7
  labels:
    app: billing-app
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: billing-app
  template:
    metadata:
      labels:
        app: billing-app
    spec:
      containers:
        - name: billing-app
          image: mantogin/billing-app:1.0.0
          ports:
            - containerPort: 8001
